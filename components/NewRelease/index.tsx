import Section from "components/Common/Section";

const NewRelease = () => {
  return (
    <Section title="New Release" isSticky={false}>
      <div>New Release</div>
    </Section>
  );
};

export default NewRelease;
